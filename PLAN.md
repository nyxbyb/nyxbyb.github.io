# 凿星图 · Caelum — 技术栈与实现规划

> 一个"个人精神版图测绘仪"：把读过的书、写过的文章、发表的论文放进同一张语义地图，
> 用来回答三个问题 —— 我被什么塑造了？我的盲区在哪里？我的输入与输出是否共振？
>
> 它是私人的、向内观看的、有审美偏见的。夜神不需要白昼的掌声，她只需要看清自己的星图。

---

## 0. 设计哲学（先于技术）

1. **静态优先，本地优先。** 这不是一个需要登录、后端、数据库服务的产品。它是一面只属于你的镜子。所有数据以纯文本/文件形式存在，可用 git 版本化，可在断网的笔记本上完整运行。
2. **数据即文本，文本即可读。** 每本书/每篇文章是一个可手工编辑的 Markdown 文件。构建脚本把它们编译成前端需要的优化格式。你永远可以直接打开、修改、理解自己的数据。
3. **重计算离线做，轻交互在线做。** 嵌入、降维、聚类这些重活全部在本地流水线里预先算好；浏览器只负责渲染和响应你的探索。
4. **克制规模，追求清晰。** 3000–5000 本经过时间淘洗的经典 + 几百篇自己的文字。这个量级小到可以"看清整张图"，又大到足以浮现结构。

---

## 1. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     本地数据流水线 (Python)                    │
│                                                              │
│  Markdown 语料 ──► 解析/清洗 ──► 嵌入(Embedding) ──► 降维      │
│  (书/文章/论文)                                  (UMAP 2D/3D) │
│                                          │                   │
│                       聚类(HDBSCAN) ◄──────┘                   │
│                              │                               │
│                    质心计算 / 盲区推荐 / 自画像生成               │
└──────────────────────────────┬───────────────────────────────┘
                               │  编译产物
                               ▼
        ┌──────────────────────────────────────────┐
        │   静态站点 (Vite + TS)                     │
        │   graph.bin/json  +  nodes.json + profile │
        │                                          │
        │   WebGL 散点图  ──►  点击节点弹出卡片        │
        │   暖色=已读 · 灰=未读 · ★=精神质心           │
        └──────────────────────────────────────────┘
                               │
                               ▼
        私有部署（本地 / Cloudflare Pages 加访问锁）
        + 一份可粘贴给大模型的"自画像" Markdown
```

**一句话**：Python 离线把语料变成坐标与洞见，前端把坐标渲染成一张可探索的星图。

---

## 2. 技术栈选型

### 2.1 数据与处理流水线（后端 = 本地脚本）

| 关注点 | 推荐 | 理由 |
|---|---|---|
| 语言 / 包管理 | **Python 3.11+ + uv** | 你是 CS 博士，Python 是母语；`uv` 极快、单文件锁定 |
| 嵌入模型 | **BGE-M3**（首选）或 OpenAI `text-embedding-3-large` | 中英混合语料（中文经典 + 英文论文 + 公众号），BGE-M3 多语言强、可本地跑、零调用费、全私有；API 方案省事且对 5k 条目一次性成本极低 |
| 嵌入运行 | `sentence-transformers` / `FlagEmbedding` | BGE-M3 官方推理库，M 系列 Mac 可走 MPS |
| 降维 | **UMAP**（`umap-learn`），备选 PaCMAP | 保留局部邻域结构，适合做"地图"；`n_components=2` 与 `3` 各出一份 |
| 聚类 / 主题 | **HDBSCAN** + c-TF-IDF（BERTopic 思路），可让 LLM 给星团命名 | 把语义相近的点聚成"星座"，并给每座星座起名字 |
| 向量存储 | **sqlite-vec**（单文件）或纯 `numpy` | 5k 向量用不上专用向量库；sqlite-vec 顺带存元数据，一个文件带走 |
| 数据处理 | `pandas` / `polars`、`numpy` | 常规 |

> **API vs 本地嵌入的取舍**：若重隐私与可复现、且不想联网 → BGE-M3 本地。若只想快速出第一版、且接受一次性把书目简介发给 API → OpenAI 批量嵌入更省事。两者产出的向量都可以缓存，后续增量嵌入即可。

### 2.2 前端可视化（核心体验）

| 关注点 | 推荐 | 理由 |
|---|---|---|
| 构建 / 语言 | **Vite + TypeScript** | 快、现代、静态产物 |
| UI 框架 | **Svelte**（或极简 vanilla） | 地图才是主角，UI 很轻；Svelte 心智负担小、产物小 |
| 2D 散点渲染 | **regl-scatterplot** 或 **Cosmograph** | 均为 WebGL，5k–百万级点流畅平移/缩放/悬停；Cosmograph 本身就是为"嵌入可视化"而生，自带 2D/3D |
| 3D（可选） | `three.js` 或 Cosmograph 3D | 建议先做 2D 做到极致，3D 作为炫技模式后加 |
| 卡片 / 排版 | 自定义组件 + **思源宋体 / Noto Serif SC** | 中式书卷气 + 暗夜星图的审美；标题可用衬线，正文清晰易读 |
| 图表配色 | 自绘 canvas，暖金（已读）/ 冷灰（未读）/ 星辉（质心） | 贴合"夜空中自行发光"的意象 |

> **2D vs 3D**：先 2D。UMAP 的 2D 投影信息损失更小、交互更直观、性能更稳；3D 适合"漫游"，但作为理解工具 2D 更诚实。把 3D 列为里程碑后期的增强。

### 2.3 部署与分发

| 关注点 | 推荐 |
|---|---|
| 托管 | **Cloudflare Pages** 或 GitHub Pages（纯静态） |
| 私密性 | 本地 `vite dev` 完全离线；公网部署用 **Cloudflare Access**（邮箱一次性验证码）或简单 Basic Auth 上锁 |
| 数据 | 嵌入若用本地 BGE-M3，则全程离线、书目不外发 |

---

## 3. 数据模型

每本书 / 文章 / 论文是一个 Markdown 文件，front-matter 存结构化字段，正文存你的书评/笔记/摘要。

```markdown
---
id: book_0241
type: book            # book | article | paper
title: 百年孤独
author: 加西亚·马尔克斯
status: read          # read | unread | reading
rating: 5             # 1–5，已读才有
read_date: 2019-04
tags: [魔幻现实主义, 拉美文学]
link: https://mp.weixin.qq.com/...   # 公众号书评链接（文章/书评用）
isbn: 9787544253994
---

这里是你的书评 / 随笔正文。嵌入时会把 标题+作者+简介+这段正文
一起编码，使节点真正承载"你与它的关系"，而不只是客观元数据。
```

**编译产物**（前端只读这些）：

- `graph.json`（或二进制）：`[{id, type, x, y, z, status, rating, cluster, title}…]`，只放渲染必需字段，体积小。
- `nodes/{id}.json`：点击卡片时按需取详情（书评正文、评分、链接）。
- `profile.md`：自动生成的"精神自画像"，可整段粘贴给大模型当上下文。
- `caelum.db`（sqlite-vec）：嵌入 + 元数据，供增量更新与推荐重算。

**书目来源（3000–5000 经典的"底图"）**：豆瓣读书 Top 系列 / 各奖项（诺贝尔、布克、茅盾、鲁迅、雨果、星云）/ 经典丛书（理想国、Penguin Classics、商务汉译名著）整理成种子清单；你自己的"已读"从豆瓣/微信读书导出后打 `status: read`。

---

## 4. 核心算法

### 4.1 精神质心（Spiritual Centroid）
- 取向量集合：`已读的书 + 你的文章 + 你的论文`。
- 加权质心：`centroid = Σ(wᵢ · vᵢ) / Σwᵢ`，权重 `wᵢ` 来自评分 / 你自己的标注（哪本"塑造了我"）。
- 文章与论文可设更高权重 —— 它们是你**主动产出**的方向，比"读过的"更能代表你的指向。

### 4.2 盲区推荐（"相见恨晚"的漏网之鱼）
- 计算所有 `status: unread` 节点到质心的余弦距离，取最近 Top-N。
- 可加多样性约束（MMR），避免推荐挤在同一小簇。
- 输出：`你尚未读、但精神上离你最近的书`。

### 4.3 输入–输出共振
- 分别计算 `读` 与 `写`（文章+论文）两个子质心。
- 用两向量的**余弦夹角**度量"输入与输出是否同向"，并在地图上画出两条质心连线。

### 4.4 星座（主题聚类）
- HDBSCAN 对降维结果聚类 → 每个簇用 c-TF-IDF 提取关键词 → 交给本地/ API 大模型起一个有文学气息的名字（如"沉默与流亡"）。
- 每座星座在图上以淡淡的光晕/边界显示。

---

## 5. 功能清单（对应需求）

| 需求 | 实现 |
|---|---|
| 三类节点同图异形 | 书=圆点，文章=三角，论文=方形（WebGL 里用不同 sprite/SDF） |
| 点击弹卡片 | hover 显示标题，click 拉取 `nodes/{id}.json` 渲染卡片：书评、评分、公众号链接 |
| 已读暖色 / 未读灰 | 渲染时按 `status` 上色，已读形成"我的光斑" |
| 精神质心标注 | 用一颗稍大的星形 marker + 微光晕，恒显 |
| 推荐未读书 | 侧栏"盲区"列表，点击飞到对应节点 |
| 私密 | 本地优先；公网部署加访问锁 |

---

## 6. 里程碑（MVP → 完整）

- **M1 · 看见第一颗星（最小闭环）**
  手工录入 ~50 本书 → BGE-M3 嵌入 → UMAP 2D → regl-scatterplot 渲染 → 点击出卡片。
  *目标：端到端跑通，确认审美方向。*
- **M2 · 星图成形**
  接入 3000–5000 经典底图；已读高亮；质心计算与标注；盲区推荐列表。
- **M3 · 三类印记同图**
  纳入文章与论文，异形区分；输入–输出共振角；星座聚类与命名。
- **M4 · 自画像与对话**
  生成 `profile.md`（你的主题、质心邻域、盲区、一句话身份陈述），作为给大模型的上下文；可一键复制。
- **M5 · 打磨与增强**
  3D 漫游模式、时间轴（按阅读年份点亮星图）、搜索、按星座过滤、美学精修。

---

## 7. 风险与注意

1. **嵌入质量决定一切。** 先用 50 本你极熟的书做"验收"：相关的书是否真的聚在一起？不满意就换模型/调嵌入文本（是否纳入书评正文、简介长度）。
2. **UMAP 的随机性。** 固定 `random_state` 保证可复现；每次全量重算会"重排大陆"，增量更新需谨慎（可用对齐/增量 UMAP）。
3. **数据冷启动最费时。** 书目清洗、豆瓣/微信读书导出、公众号文章抓取是体力活，建议写一次性脚本并缓存原始响应。
4. **中文嵌入。** 务必选多语言强模型（BGE-M3 / e5-multilingual），纯英文模型会把中文经典压成一团。
5. **别过度工程。** 5k 节点用不上向量数据库集群、后端服务、登录系统。保持"一个人能维护十年"的克制。

---

## 8. 建议的最终栈（一页速览）

```
流水线   Python 3.11 + uv · BGE-M3(sentence-transformers) · UMAP · HDBSCAN
存储     Markdown(front-matter) → graph.json + nodes/*.json + sqlite-vec + profile.md
前端     Vite + TypeScript + Svelte · regl-scatterplot/Cosmograph(WebGL) · Noto Serif SC
部署     静态 · Cloudflare Pages(加 Access 锁) 或完全本地
特色产出 profile.md —— 你的"文字存在论"自画像，可粘贴给任何大模型
```

> 夜神不需要白昼的掌声。这张图的每一个坐标，都是你对自己精神来路的一次确认。
